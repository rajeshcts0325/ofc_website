import RegisterForm from "@/components/ui/register/RegisterForm";

export const metadata = {
  title: "Regsiter Your Account || Create Your Account Now",
  description: "Register Your Account Now",
};


const Page = () => {
  return <RegisterForm />;
};

export default Page;
